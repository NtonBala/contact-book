import {ADD_CONTACT_SUCCESS, EDIT_CONTACT_SUCCESS} from "../constants/actionTypes";

const contacts = (state = [], action) => {
    const {type} = action;

    switch (type) {
        default:
            return state;
    }
};

export default contacts;
